coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: "condensed_header, condensed_files, components, flags, condensed_footer"
  hide_project_coverage: true

component_management:
  individual_components:
    - component_id: foton
      name: foton
      paths:
        - "src/**"
    - component_id: xtask
      name: xtask
      paths:
        - "xtask/src/**"
