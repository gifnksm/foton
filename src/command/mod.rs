pub(crate) use self::{
    generate_man::*, info::*, install::*, list::*, print_completion::*, search::*, uninstall::*,
    update::*,
};

mod generate_man;
mod info;
mod install;
mod list;
mod print_completion;
mod search;
mod uninstall;
mod update;
