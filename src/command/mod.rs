pub(crate) use self::{
    generate_man::*, info::*, install::*, list::*, print_completion::*, uninstall::*, update::*,
};

mod generate_man;
mod info;
mod install;
mod list;
mod print_completion;
mod uninstall;
mod update;
